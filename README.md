# rrg-pro

```
"devDependencies": {
   "babel-plugin-import": "^1.13.0", //一个按需加载组件代码和样式的babel插件
   "react-app-rewired": "^2.1.5", //需要对 create-react-app 的默认配置进行自定义
   "customize-cra": "^0.9.1", //需要customize-cra配合react-app-rewired
   "typescript": "^3.7.5",   //源码中何处是ts？
 }
}
//装饰器（@withRouter可将路由路由参数传到组件的this.props中)
//装饰器  @connect(mapStateToProps, mapDispatchToProps)
```
```
/public 文件夹是create-react-app生成的默认文件夹：网站图标等网站概述性文件
/src 文件夹下根目录文件都是默认文件。
```